import { Grid } from "@mui/material";
import React from "react";

function BaseIcon({ direction }) {
  return (
    <Grid
      container
      direction={direction}
      justifyContent="flex-start"
      alignItems="flex-start"
    >
        
    </Grid>
  );
}

export default BaseIcon;
